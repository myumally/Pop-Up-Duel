#ifndef enum__hpp
#define enum__hpp

enum PlayerType {Opponent, NotOpponent};

enum ZoneType {Blank, Sword, Shield};

enum Colour {Grey, Red, Green, Blue, Yellow};

enum Effect {Nothing, Burn, Freeze, Numb, Poison, Silence, Protect, Refresh, Haste, Slow};

#endif
